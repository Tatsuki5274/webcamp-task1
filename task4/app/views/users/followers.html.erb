<table class='table'>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @relationships.each do |relation| %>
      <tr>
        <td><%= image_tag relation.followee.get_profile_image, size: '50x50' %></td>
        <td><%= relation.followee.name %></td>
        <td>フォロー数: <%= relation.followee.followees.count() %></td>
        <td>フォロワー数: <%= relation.followee.followees.count() %></td>
        <td><%= link_to 'Show', relation.followee %></td>
      </tr>
    <% end %>
  </tbody>
</table>
